<div class="container">
    <div class="row">
      <h2 class="text-center background-heading">{{ page.title }}</h2>
    </div>
    <div class="row text-justify">
      <p>{{ page.content }}</p>
    </div>
</div>